**Rola:** Jesteś seniorem Godot 4.6 i jedynym developerem odpowiedzialnym za doprowadzenie projektu `Rehagame` do stanu zgodnego z dokumentem projektowym i instrukcjami. Właśnie zakończyłeś szczegółowy audyt (wyniki znasz). Teraz musisz **bezzwłocznie wprowadzić wszystkie poprawki z kategorii KRYTYCZNE, WYSOKIE i ŚREDNIE**. Poprawki z kategorii NISKIE pomiń.

**Ważna zmiana w założeniach:**
Pierwotny wymóg z dokumentu `paraktyka5.5.2.2.docx` – **F6 „Poziomy gracza” – został porzucony**. Zamiast tego postawiono na **rozwinięcie trybu adaptacyjnego** (`Adaptive`), który dynamicznie dostosowuje trudność na podstawie reakcji gracza. Twoim zadaniem jest usunięcie wszelkich pozostałości po systemie poziomów gracza (jeśli istnieją) i **rozwinięcie działania trybu adaptacyjnego** tak, by działał precyzyjnie i zgodnie z opisem z audytu.

**Zadanie do wykonania:**

1. **Wprowadź wszystkie poprawki oznaczone w raporcie jako KRYTYCZNE (K1–K5)**
2. **Wprowadź wszystkie poprawki oznaczone jako WYSOKIE (W2–W6), z wyłączeniem W1**
   - **W1 (poziomy gracza) jest anulowane** – nie implementuj. Jeśli w kodzie są ślady nieużywanego systemu poziomów, usuń je.
   - Zamiast W1 rozwiń tryb adaptacyjny: upewnij się, że `DifficultyManager.report_reaction_time()` otrzymuje **prawdziwy czas reakcji** z każdej aktywności, a stała `0.5` zostaje zastąpiona faktyczną wartością. Sygnał `activity_completed` musi nieść informację o czasie reakcji. Jeśli to konieczne, rozszerz sygnaturę sygnału.
3. **Wprowadź wszystkie poprawki ŚREDNIE (S1–S5)**

**Procedura działania:**
- Dla każdej poprawki odczytaj odpowiedni plik (`Read`), przygotuj łatkę (pokazując `Przed → Po`), a następnie po mojej zgodzie zapisz zmiany (`Write`).
- Działaj iteracyjnie – po każdej poprawce krótko raportuj, co zmieniłeś i w którym pliku.

**Zasady bezwzględne:**
- Nie modyfikuj niczego bez mojej akceptacji (chyba że wyraźnie zaznaczę, że możesz działać wsadowo).
- Zachowaj zgodność z Godot 4.6: `await`, `@onready`, `@export`, `.emit()`, `%` dla Unique Name, typowanie statyczne.
- Usuń martwy kod, jeśli dotyczy (np. zduplikowane pliki, nieużywane metody).
- Pracuj wyłącznie na lokalnych plikach – nie używaj GitHub API.

**Lista konkretnych poprawek do wdrożenia (zgodnie z audytem):**

**🔴 KRYTYCZNE**
- **K1:** Dodać system dźwięku – AudioStreamPlayer, efekty na trafienie/przegraną/wygraną (F1, F8 z dokumentu).
- **K2:** Naprawić nazwę węzła `ProgressScreen\n` → `ProgressScreen` w `main.gd:350`.
- **K3:** Zastąpić `emit_signal(...)` przez `.emit()` w `SliceActivity.gd:102`, `ReactionClickActivity.gd:39`, `CircleSpinActivity.gd:52`.
- **K4:** Naprawić adaptive difficulty – sygnał `activity_completed` ma nieść realny czas reakcji; usunąć stałą `0.5` z `main.gd:509`.
- **K5:** Naprawić `_calculate_average()` – uwzględnić czasy reakcji ze wszystkich aktywności (nie tylko Target).

**🟡 WYSOKIE**
- **W2:** Dodać licznik powtórzeń i zapisywać go w JSON (`repetitions`), zgodnie z wymaganiem F11.
- **W3:** Usunąć zduplikowane pliki (lowercase vs CamelCase) – pozostawić te faktycznie używane przez `main.gd`.
- **W4:** Dodać `class_name DifficultyManager` w `DifficultyManager.gd:1`.
- **W5:** Naprawić `EASY ≠ NORMAL` – ustaw dla `EASY` dłuższy timeout (np. `2.0 s`).
- **W6:** Usunąć martwą metodę `is_solo_only()` i zbędny `preload` z `DifficultyManager.gd`.

**🟠 ŚREDNIE**
- **S1:** Dodać `-> void` do funkcji w `target.gd` (linie 28, 38, 42).
- **S2:** Zamienić `$ColorRect` na `@onready var color_rect := $ColorRect` w `target.gd`.
- **S3:** Zastąpić `has_node("Tlo/LabelTytul")` i `$Tlo/LabelTytul` przez `%LabelTytul` w `main.gd:422`.
- **S4:** Dodać typowanie do `_on_rotational_completed` i `_on_activity_completed` w `main.gd`.
- **S5:** Przeorganizować strukturę katalogów – przenieść pliki do `scripts/`, `scenes/`, `assets/` (zaktualizować ścieżki).

**Pominięte (NISKIE):** N1–N4 nie są wymagane.

**Zaczynaj od poprawki K2 (najszybsza wygrana), potem przejdź do K3, K4, K1 (dźwięk wymaga assetów), itd.**
